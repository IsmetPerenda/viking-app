import classes from './MealsSummary.module.css';

const MealsSummary = () => {
  return (
    <section className={classes.summary}>
      <h2>Take only memories, leave only footprints</h2>
      <p>
Pick your favourite places to visit,one package is for one person.
      </p>
      <p>
        All our packages are specialy planned, simple and
        of course by experienced guides!
      </p>
    </section>
  );
};

export default MealsSummary;
